# Copy to /etc/jarvis/restic.env on the server (mode 0640). Never commit.
# Create a private B2 bucket, then an application key limited to that bucket.

B2_ACCOUNT_ID=
B2_ACCOUNT_KEY=
RESTIC_REPOSITORY=b2:jarvis-backups:restic
RESTIC_PASSWORD=
