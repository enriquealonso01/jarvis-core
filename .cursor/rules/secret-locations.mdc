---
description: Where Jarvis operator secrets live on Enrique's laptop; never print or commit them
alwaysApply: true
---

# Jarvis secret locations

Laptop (this machine only): `C:\Users\Enrique\.jarvis\secrets\`

- `b2-restic.env` — B2 + restic
- `control-center-login.txt` — Control Center login
- `netcup-root.txt` — VPS root/console (SSH is keys-only)

Never print file contents, never commit them, never paste them into chat. The VPS stores `backup_b2` encrypted in Postgres; restic CLI reads `/etc/jarvis/restic.env`.
