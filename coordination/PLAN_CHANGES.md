# Plan changes — requirement intake log

Written only by the Requirements session (Enrique + Claude). Append-only.
Builder and Tester read this every loop to notice what changed in the plan.

Format per entry:
`YYYY-MM-DD  —  <what Enrique asked for>  —  steps touched: <S.. / new step>  —  affects already-built: yes/no (if yes, Builder rebuilds + Tester re-verifies)`

---

2026-09-03  —  Live browser voice UI: a "Talk to Jarvis" button beside "Ask Jarvis" that opens a full-duplex in-app call with an audio-driven moving-circle orb, same capabilities as WhatsApp/phone (reuses the S21 runtime; browser is a new transport + UI, not a second conversation brain).  —  steps touched: new S49 (references S19–S24, S39, IV.0/0.45; not edited)  —  affects already-built: no

2026-09-03  —  Jarvis places outbound calls to a third party to complete a task (e.g. "call this restaurant, book Saturday 8pm for two; come back if there's a problem"). Finding the place is S32; this is the call that acts on the pick.  —  steps touched: new S50 (reuses S23 dialer, S32 browser, S42 external-impact approval, S10 grants, S21 runtime, S41 disclosure; those steps not edited)  —  affects already-built: no

2026-09-03  —  Home is too dense and scrolls: make it a single no-scroll map of the whole system (a node-link "constellation" of projects, memory, running work, needs-you; build-progress bar stays), with all detail one click away. Every point bound to a real object; motion only on real state change (I.3 / 0.45) — not a movie prop.  —  steps touched: new S51 (revises S13's Home layout; keeps S13b bar; applies S15/I.3; adds back-ref banner to S13)  —  affects already-built: YES — Builder rebuilds Home, Tester re-verifies S13/S13b on the box

2026-09-03  —  "Jarvis must have access to everything and grow its own capabilities — infinite capabilities, smart and forthcoming, like Iron Man's Jarvis." State the missing founding PRINCIPLE: access-to-everything + self-extension, reconciled with the isolation spine (broad reach is safe *because* of the walls; the one thing it can't do alone is ship its own control plane; paying for things is a future spend-ceiling-with-approval, not now).  —  steps touched: Part 0 — 0.3 gains a 4th property "It grows"; new §0.35 (ties together existing S16/S31/S43/S44/S46). Additive principle, no built contract changed.  —  affects already-built: no

2026-09-03  —  Forthcoming behaviour: Jarvis asks for what it needs BEFORE a task is blocked (a key over WhatsApp, a tappable sign-in), proposes capabilities with one-tap approve, writes+registers its own reusable skills; and a scheduled call opens with its prepared purpose ("you asked me to call about X — where do you want to start?"), not a blank greeting. Bounded by S33's reasons list so it never nags.  —  steps touched: new S52 (reuses S16/S44/S46/S31; references S23 scheduled-call subject; not edited)  —  affects already-built: no

2026-09-03  —  Agent orchestration / powerhouse engineering: told in plain language ("spawn a planner that loops until the plan is solid, then a builder that loops and starts building"), Jarvis runs many cooperating agents on one goal — concurrent worktrees/branches, coordinating through recorded state, loops driven by a done-condition not a clock. Governed: ADR 007 leasing + concurrency ceiling, spend ceiling, watchdog, one kill switch; every agent inside the project's lines; none can ship core alone.  —  steps touched: new S53 (reuses S5/S6/S9/S28/S11; generalises S9's two-role case)  —  affects already-built: no

2026-09-03  —  Round-1 base skill library approved (first-party, vetted set: Anthropic document skills docx/pdf/pptx/xlsx + skill-creator; self-hosted Firecrawl for scraping; Playwright for the browser; Context7 for library docs). Brave web-search DROPPED (paid). Read-only DB skill DEFERRED — no official MySQL skill exists; to be handled on its own track. Two S32 decisions: Mode-2 scraping backend = self-hosted Firecrawl (read-only scrape/search/crawl/map/parse only; its interact/agent surfaces gated as Mode 1), and browser = Playwright with browser-use EXPLICITLY REJECTED (a second agent that bypasses the model router + broker gate, overlaps Playwright, solves CAPTCHAs). Full vetting — versions to pin, self-host confirmation, licences, red flags — in docs/SKILLS_RESEARCH_ROUND1.md + docs/SKILLS_ROUND1_APPROVED.md.  —  steps touched: S32 (edited: names self-hosted Firecrawl + Playwright as chosen backends, resolves an ADR-016 open choice; ADR 016 itself still to be written); base skill library relates to S31/S44/S52/§0.35 (not edited)  —  affects already-built: no (S32 not yet built/verified; Builder folds Firecrawl into its in-progress S32 Mode-2 work)

2026-09-03  —  REVERSAL of the one withheld autonomy: Enrique decided Jarvis should be able to deploy its OWN control plane (jarvis-core) autonomously too — the previous plan made that the single always-confirm exception. Folded in as a capability WITH the recoverability envelope the plan already specified (staged canary → S8+S29 suites → health-gated promote → auto-rollback from OUTSIDE the deploy → expand-contract so rollback never restores the DB → Maintenance watches). One narrow residual gate kept (already at VII.5): changes to the safety surface — isolation/auth/backups/spend/kill-switch — still need approval, because a wrong change there could disable the ability to stop it. Revised every place that asserted the old "never deploys itself" rule so the plan stays consistent.  —  steps touched: new S54; revised §0.35, S43 (console-vs-core), S44 (extending-vs-modifying), S53 (agent bullets), and "After S37 — the handover" (the authority bullet + "in scope now")  —  affects already-built: no (S43/S44/S53/S54 not yet built; the handover invariant is a plan rule, now revised — if any built code enforces "refuse core self-deploy", that enforcement changes and Tester should flag it)

2026-09-03  —  PRODUCTION BUG: Jarvis has called Enrique several times and opened with only a greeting ("good afternoon, sir") without ever stating why it called. S23 already requires "who it is and why, in one sentence" and had a test for it, yet S23 is marked verified — a verified-but-broken gap. Hardened S23 so stating the prepared reason is mandatory and the test asserts on the CONTENT of the opening utterance (a bare greeting fails); added a Debug note with the likely root cause (reason not threaded into the opening turn / swallowed by the identity-confirm step).  —  steps touched: S23 (Build line, Test line, Debug, Done-when hardened)  —  affects already-built: YES — Builder fixes the opening-utterance path, Tester re-verifies on a REAL call that the reason is spoken first (not that a call merely connected)

2026-09-03  —  Voice agent is too slow on open-weights; Enrique wants it much faster and will pay. Decision: the realtime conversational turn (Tier 1 on a live call) becomes its own LATENCY-FIRST route, allowed/expected to use a paid low-latency model — the one role where the plan trades open-weights portability for speed. Keeps an open-weights fallback (degrade latency, not drop the call), metered with a visible ceiling, measured against S21's p50/p95 on real calls; guard: verify the slow leg is the answer-model, not TTS render (pre-render acks per S21) before swapping.  —  steps touched: S25 (new "realtime voice" subsection + Done-when), VI model-role table (added Realtime-voice row), S21 (Build bullet pointing at the route)  —  affects already-built: YES — Builder adds the realtime voice route, Tester re-verifies call latency against target
